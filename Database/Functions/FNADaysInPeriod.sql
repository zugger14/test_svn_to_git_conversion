SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO

IF EXISTS(SELECT * FROM sys.objects WHERE [object_id] = OBJECT_ID(N'[dbo].[FNADaysInPeriod]') AND TYPE IN (N'FN', N'IF', N'TF', N'FS', N'FT'))
	DROP FUNCTION [dbo].[FNADaysInPeriod]
GO

CREATE FUNCTION [dbo].[FNADaysInPeriod]()
	RETURNS INT
AS
BEGIN
	RETURN 1
END
GO