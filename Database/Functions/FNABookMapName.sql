SET ANSI_NULLS ON
SET QUOTED_IDENTIFIER ON
GO

IF EXISTS(SELECT * FROM sys.objects WHERE [object_id] = OBJECT_ID(N'[dbo].[FNABookMapName]') AND TYPE IN (N'FN', N'IF', N'TF', N'FS', N'FT'))
	DROP FUNCTION [dbo].[FNABookMapName]
GO

CREATE FUNCTION [dbo].[FNABookMapName]()
	RETURNS INT
AS
BEGIN
	RETURN 1
END
GO